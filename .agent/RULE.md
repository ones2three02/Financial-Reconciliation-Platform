# 智能体编码守则 (RULE.md)

任何协助开发本项目的 AI Agent 必须无条件遵守以下开发底线：

1. **禁止直接物理删除数据**：无论是原始文件、清洗后数据还是结果，一律通过逻辑标志进行软标记（如 valid/invalid 或 status），必须保留审计痕迹。
2. **Controller 保持极简**：API 控制器层绝不写任何 Excel 处理或算术对账等业务逻辑，逻辑必须内聚于 services 服务层。
3. **测试驱动**：新添加的任何解析器或清洗处理器，必须附带对应的 pytest 单元测试，确保其能够覆盖非法日期与金额的极端情况。