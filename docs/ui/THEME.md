# 样式主题定义 (THEME.md)

基于 TailwindCSS 的配色定义，对应 `tailwind.config.js`：
* `primary-500` (主色): `#5c7aff`
* `sidebar` (侧边栏背景): `#1e293b`
* `consistent` (一致): `emerald-600`
* `discrepancy` (异常): `rose-600`
* `missing` (缺失): `amber-600`
* 主体字体大小采用 `14px (text-sm)`，大屏数字为 `24px (text-2xl)`。