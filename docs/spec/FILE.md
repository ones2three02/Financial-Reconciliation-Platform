# 文件管理规范 (FILE.md)

## 文件暂存规则
* 用户上传的 Excel 文件暂不保存在服务器磁盘（避免磁盘碎文件积压），而是直接读入内存的 `BytesIO` 字节流，解析完成后直接存入 `raw_data` 表的 JSON 字段中。
* V2 阶段如需持久化原始文件备份，将统一规范存储在 `backend/storage/uploads/` 目录下，并按年-月-日分文件夹进行归档。