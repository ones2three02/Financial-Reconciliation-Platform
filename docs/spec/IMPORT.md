# Excel 导入规范 (IMPORT.md)

## 导入规则约束
1. **多文件支持**：文件导入中心必须支持一次性拖入或选择多个 Excel 文件。
2. **分类上传**：每个上传请求必须明确指定数据源所属的通道分类：`tonglian`、`meituan`、`douyin`、`cash` 或 `sales`。
3. **类型防呆**：只允许上传扩展名为 `.xlsx` 和 `.xls` 的工作簿。对于不合法的格式在前端限制并在后端进行 MIME 类型校验。