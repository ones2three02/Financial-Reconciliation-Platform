# 异常管理规范 (EXCEPTION.md)

## 异常异常中心闭环规则

1. **标记解决流程**：财务人员找到异常门店后，可双击或点击核实备注按钮，填写偏差成因（如美团扣率滞后、退款未入账等）。
2. **状态保护**：将 `is_resolved` 标记为 true 后，该条对账记录在界面上将不再归入默认的“未处理异常”列表中，但在大屏趋势中仍会真实保留其差额。
3. **审计留痕**：任何修改备注和解决状态的操作，必须自动记录 `resolved_by` (操作人) 及 `resolved_at` (操作时间)。