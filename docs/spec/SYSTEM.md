# 系统总体规范 (SYSTEM.md)

FRP（财务自动对账平台）必须严格遵守以下系统级别设计约束：

1. **无破坏性修改原则**：任何数据处理逻辑都严禁修改或物理删除上传的原始 Excel 附件或 Raw 数据库记录。
2. **完全可重算性 (Idempotency)**：系统对同一批文件、同一账期支持无限次重新清洗与重新对账，每次运行结果必须保持幂等。
3. **数据一致性**：对账的计算数据来源必须全量基于 `clean_data` 层的记录，禁止跨过清洗层直接拿 raw 原始数据汇总。